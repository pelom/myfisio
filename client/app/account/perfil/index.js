'use strict';

import angular from 'angular';
import PerfilController from './perfil.controller';

export default angular.module('myfisioApp.perfil', [])
  .controller('PerfilController', PerfilController).name;
